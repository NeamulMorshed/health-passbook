Omra v2.11.0+34 — Doctor dashboard fix

• Fixed doctor home page showing "Failed to load" — appointment stream now sorts
  client-side, removing the composite Firestore index dependency entirely
• Made appointment parsing resilient — one malformed document can no longer crash
  the entire stream for both doctor and patient appointment views
• Fixed patientRating field cast — handles both int and double storage safely
